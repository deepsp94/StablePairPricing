# StablePairPricing

Setup:
```
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

Run the simulation:
```
python mm.py
```

For visualization, run the notebook vis.ipynb